import { css } from '@emotion/react';

const announcementList = css`
  width: 90%;
  overflow: auto;
`;

const announcementListItem = css`
  margin: 10px 10px;
`;

export default { announcementList, announcementListItem };
