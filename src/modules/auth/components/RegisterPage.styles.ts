import { css } from '@emotion/react';

const registerPageContainer = css`
  position: absolute;
  top: 100px;
  left: 50%;
  transform: translate(-50%, 0);
`;

const registerPagePaper = css`
  padding: 10px;
`;

export default { registerPageContainer, registerPagePaper };
