import { css } from '@emotion/react';

const container = css`
  position: absolute;
  top: 100px;
  left: 50%;
  transform: translate(-50%, 0);
`;

const paper = css`
  padding: 10px;
`;

export default { container, paper };
