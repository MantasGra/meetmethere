import { css } from '@emotion/react';

const loginPageContainer = css`
  position: absolute;
  top: 100px;
  left: 50%;
  transform: translate(-50%, 0);
`;

const loginPagePaper = css`
  padding: 10px;
`;

export default { loginPageContainer, loginPagePaper };
