import { css } from '@emotion/react';

const submitContainer = css`
  text-align: right;
`;

const submitButton = css`
  margin: 10px 0;
`;

const helperTextRight = css`
  text-align: right;
`;

export default { submitContainer, submitButton, helperTextRight };
