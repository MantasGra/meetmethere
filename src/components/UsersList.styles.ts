import { css } from '@emotion/react';

const userList = css`
  width: 100%;
  max-height: 200px;
  overflow: auto;
`;

const userListName = css`
  width: 90%;
`;

export default { userList, userListName };
