export const enum Routes {
  Home = '/',
  Login = '/login',
  Register = '/register',
  Meetings = '/meetings',
  MeetingPage = '/meetings/:id',
  History = '/history',
  Invitations = '/invitations',
}
